{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "host-switcher",
  "description": "Minimal permissions for the DisplayMux host switcher",
  "windows": ["host-switcher"],
  "permissions": ["core:default"]
}
