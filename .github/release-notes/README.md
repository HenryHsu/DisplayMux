# Release notes

Before publishing a version, add both files using the exact release tag:

- `vX.Y.Z.en.md`
- `vX.Y.Z.zh-TW.md`

The release workflow intentionally stops before publishing when either language is missing. Keep headings inside these files at level three or lower because the workflow adds the language headings and direct-download section.
